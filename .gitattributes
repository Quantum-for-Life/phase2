# Set the default behavior, in case core.autocrlf not set.
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.c text
*.h text

# Jupyter notebooks aren't really readable
*.ipynb binary
